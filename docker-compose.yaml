version: "3.2"

services:

  adegasapi:
    container_name: adegasapi
    image: adegasapi
    networks:
      - default_network
    ports:
      - 8020:8020
    environment:
      PORT: 8020
      MONGO_HOST: mongo
      MONGO_PORT: 27000
      MONGO_DATABASE: adegas
      MONGO_PASSWORD: dlbMb4LpT30diFiZ6j930Rrl76yd6OpZ

networks:
  default_network:
    driver: bridge